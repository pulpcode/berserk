# W01-5 实施与验收记录

日期：2026-09-18。状态：**实现完成，E01～E15 组合验收通过；待用户查看与 Git 提交**。不代表完整 W01、正式多用户平台或最终消防业务 MVP 已交付。

## 已落地

- 任务 × 席位索引 v2、旧数据显式备份迁移。
- 普通工作目录、流式上传、同名不覆盖、取消核对、安全文件访问与固定下载副本。
- Pi 公开 read/write/edit/bash/ls/find 工厂、请求级 Docker 后端、只读子角色、日志映射。
- 原生附件引用与下载记录、上传和文件面板、历史引用状态、交付卡片。
- 同目录多会话并行；停止与清理归到具体请求，未增加工作区整轮队列。

## 本地证据

- TypeScript、ESLint／导入边界检查、生产构建通过。
- 浏览器完整回归：40 项通过（2 workers）；含原有对话、AGENTS 编辑、压缩、子 Agent 及新增文件流程。
- 单元／Pi 集成全量测试：23 个文件、176 项通过（2 workers）。高并行与浏览器同时运行曾产生超时，分离复核后通过，未扩大测试时限。
- 最终复核修复了 241～255 字节普通文件名可交付但不可下载的问题，新增 250 字节名称回归。修复后文件服务 8 项测试通过（含新增 1 项），TypeScript、Lint 和差异检查再次通过；同套 8 项在 Ubuntu 通过。
- 桌面与 375px 文件面板已检查；真实本地页面能读取旧会话、子任务和空文件目录。
- 本地旧数据两个工作区已停服迁移，完整备份位于被 Git 忽略的 `.backups/before-seats-20260918-w01-5`；29 个原有文件除索引／标记外逐字节一致。

本地 UI 回归使用受控 API，Pi 集成使用真实 Pi 与确定性模型／Docker 控制替身，均不代替 Linux 容器实测。

## Ubuntu 验证

用户提供 SSH 别名 `tencent-server`。使用独立目录 `/home/ubuntu/berserk-w01-5.gIXPCA`；未修改现有 MySQL／Redis 服务。

- Ubuntu 环境：Linux x86_64，UID/GID 1000，Docker 26.1.3，Python 3.10.12，开始时可用磁盘约 28 GB。
- Node.js 24.14.0 从官方发行站点下载，SHA-256 与官方 SHASUMS256 核对一致，安装在独立目录内。
- 应用依赖安装完成，Linux 文件／API／Pi 集成补充检查：6 个文件、24 项通过。
- 执行镜像构建完成：`sha256:b32432c33720afe2b0f8d6f6f72378306152c2c2bc19323ba384b654ce1cdc76`。构建期使用腾讯云 HTTPS Debian／PyPI 镜像源，保留 Debian 签名校验；默认构建配置仍使用官方源。
- 真实 `probe-execution.ts` 退出码 0：同席位共享／跨席位隔离，非 root、只读系统、断网、无凭证、路径限制和只读日志；CSV／JSON／XLSX／DOCX／文本 PDF／中文 PNG；360KB 日志完整留存；非零退出与超时后续作；setsid 后代清理；停止一个请求不误伤另一容器；文件持久化与本实例残留容器清理。
- 用户明确允许后，现有模型配置经 SSH 保存到应用 `.env.local`（0600）。API Key 不进入 Git、验收报告或执行容器。

## 真实模型与网页

使用 DeepSeek `deepseek-flash`、Pi 0.85.1 和上述镜像，全部使用独立测试数据。

- `probe:files` 退出 0：HTTP 上传 CSV → 实际运行 Python → JSON／Markdown／XLSX／DOCX／PDF／中文 PNG；金额 60 改为 65；旧下载副本不变；reviewer 实际只读检查；另一会话读同目录；重新打开服务保留历史与下载，无重放。
- `probe:files-continuation` 退出 0：保持真实模型容量，只调整隔离测试目录的原生 R/K，触发一次 Pi 默认自动压缩，再实际修改／执行原任务，金额由 60 改为 65；历史与文件原文保留。取消真实长命令后父进程及 setsid 子进程均停止写入，容器销毁；下一轮读取不重放脚本。
- 浏览器通过 SSH 转发访问 `http://127.0.0.1:4315/`：上传 CSV → 生成并执行 `analyze.py` → 合计 70 → 点击首次下载 → 修改甲的金额 → 重跑合计 75 → 点击新下载。两次均收到浏览器下载事件，API 下载字节与保存的大小／SHA-256 一致，旧内容仍保留 70。
- 网页实际运行长命令后点击停止，终态 `cancelled`，`browser-stop.log` 固定为 185 字节；等待后 hash 不变，无该请求容器。文件面板预览 `browser-result.json` 显示 75。服务加载最终修复重启后，历史、文件与两份下载保留，无命令重放。

浏览器样本归属：workspace `459a9e68-441c-4d4f-b471-7408ff0e5e93`，session `01a0b2a0-e14b-7129-a499-429d7ee4ae1a`。两个文件处理 request 分别为 `6da3ebc7-44c1-4e42-8a44-d63f14c5f288`、`7a008117-88f4-47e3-8cd3-42ef7c401fee`；取消 request 为 `8403c8f2-aab0-4045-aad9-2d5d71c86211`。

原始证据保存在服务器 `/home/ubuntu/berserk-w01-5.gIXPCA/evidence/`：`execution-probe.json`、`files-evidence.json`、`files-continuation-evidence.json`、`browser-files-evidence.json`。包含请求／工具结果、文件大小与 hash、模型用量及停止核对；未保存凭证。执行镜像与依赖明细见 [执行环境记录](execution-validation.md)。

## 验收对应

| 编号 | 组合证据 |
| --- | --- |
| E01 | 席位索引／迁移测试、原数据 29 文件字节核对、真实容器跨席位隔离 |
| E02 | 真实模型多格式脚本处理及网页 70→75 |
| E03 | Pi 工具测试与真实 P0 非零退出／超时续作、360KB 输出、格式解析生成；文本模型图片拒绝测试 |
| E04 | 上传服务／API 边界测试、原字节核对、真实网页上传 |
| E05 | 浏览器附件切换／刷新／迟到响应／取消竞争回归、真实普通文件落盘 |
| E06 | Pi 输入证据校验、附件变更／缺失／外部 ID 测试与真实按需读取 |
| E07 | 安全预览测试、真实两轮固定副本、网页下载事件与 API 字节一致 |
| E08 | 迁移与恢复故障测试、P0 残留清理、真实模型重新打开与网页服务重启 |
| E09 | 真实原生压缩后文件续作、日志映射测试及长输出实测 |
| E10 | 并行集成回归、P0 同目录不同容器独立执行／停止、上传不等待整轮 |
| E11 | 真实父／子进程取消、双重 fork 清理、网页停止后日志稳定、其他容器继续 |
| E12 | 子角色工具权限测试、真实 reviewer 读取当前文件、旧历史回归 |
| E13 | 路径／符号链接／席位／凭证边界测试与真实非 root／断网／只读根目录 |
| E14 | 40 项浏览器回归、375px 与桌面检查、真实文件列表／预览／下载 |
| E15 | 浏览器→真实 API→Pi→Docker→DeepSeek 完整上传、执行、多轮、下载及停止 |

仍保留设计边界：固定测试席位，尚无真实登录与交接；同文件并发写入不保证冲突保护；无成果上报／审批状态；执行默认断网，镜像内只保证所列基础格式能力。
