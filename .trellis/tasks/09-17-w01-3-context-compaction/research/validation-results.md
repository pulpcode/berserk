# W01-3 实施与验收记录

日期：2026-09-17。W01-3/S2b 已实现并通过组合验收；完整 W01、S2c 与 S3 尚未完成。

## 交付

- 开启 Pi 0.85.1 默认自动压缩，不增加自定义摘要提示或格式。旧对话和工具结果保留，摘要按原生条目追加；当前 AGENTS.md 仍独立加载。
- 移除默认累计工具／模型调用次数与整轮时长限制，独立处理模型空闲、可选单次请求、工具和整轮时限，保留原生有界重试及一次溢出恢复。
- 网页显示压缩状态，支持停止、最近摘要与本轮摘要详情。模型设置增加 C/M/R/K、参数来源及未知容量提示，兼容旧配置。
- 保存失败停用污染的会话管理器；严格校验原生行、保留起点及摘要归属，避免误恢复或错误关联。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| TypeScript | 通过 |
| ESLint 与 Pi／浏览器依赖边界 | 通过 |
| 确定性测试 | 12 个文件、89 项通过 |
| 浏览器全量回归 | 27 项通过 |
| 生产构建 | 通过 |
| Git 空白与任务上下文检查 | 通过 |

确定性测试实际运行 Pi 会话与默认压缩入口，替换模型流或 HTTP；包含 35 次工具、36 次模型调用持续完成，虚拟时间超过 120 秒可继续、显式整轮时限、单次提供方超时、HTTP 心跳／停滞、三次额外重试、一次溢出恢复、双摘要／同轮多次压缩、工具结果截短、取消竞争和写失败。

## 真实 DeepSeek 验收

最终独立目录运行共 15 次用户请求：14 次成功、1 次按预期取消；20 次模型尝试，其中回答 15 次、摘要 5 次。成功保存 2 条原生摘要，各由 2 次原生摘要调用形成；另 1 次摘要在执行中取消。已返回的实际用量合计 86,198 token，取消调用未返回用量，不能据此推算总费用。

保持真实 C=1,000,000、M=393,216，通过隔离 R/K 提前触发；探针单轮 300 秒时限不写回产品配置。完整记录及摘要内容见 [脱敏证据](live-evidence.json)。原始探针目录：`/var/folders/td/741qp46d52b2b8_2zdzj7ps40000gn/T/berserk-compaction-live-8Puiwb`，文件 hash 已记录。

| 用例 | 实际结果 |
| --- | --- |
| 两项最新纠正 | 压缩后采用 37 人、45 分钟，保留青松项目及尚未生成的执行检查清单 |
| 真实资料工具 | 实际读取 meeting-notes 的长文本，磁盘和网页原文保留；后续回答区分资料原始参数与当前方案参数 |
| 再次压缩 | 新增本会话事实“海棠厅”进入后续摘要，原消息 ID／内容未改写 |
| 当前指令 | 甲标记改乙后，回答仅采用乙；清空 AGENTS 后甲乙均未复现 |
| 摘要中停止 | 观察到原生压缩开始并实际进入摘要尝试后取消，结果为 cancelled；随后可继续对话 |
| 独立进程重启 | 恢复摘要后正确回答项目、37 人、45 分钟、海棠厅及待交付检查清单 |
| 真实网页 | 查看摘要时 POST=0；375px 无横向溢出；网页发送一次后成功续聊，POST=1 |

真实页面截图已人工检查；手机长标题下的模型名称改为单行省略，避免被挤成多行。只读复验不调用模型。

## 验收映射

| 编号 | 证据与结论 |
| --- | --- |
| B01 | model-config、model-settings、server 与页面设置测试：迁移、严格数值、身份切换、未知容量、冲突和忙碌保护通过 |
| B02 | compaction 测试捕获原生上下文与调用参数：请求前、工具循环／结束检查、近期保留区、合法输出额度通过 |
| B03 | 真实首次摘要及续作保留最新两项纠正和未完成交付物，通过 |
| B04 | 确定性核验近期原文随后进入摘要、同轮多条新 ID、双摘要一次保存；真实再次压缩和原文不变通过 |
| B05 | provider-payload 与真实甲→乙→删除：本轮快照固定、当前指令有效，通过 |
| B06 | server、页面和真实 GET：原文保留、摘要详情只读、历史资料独立，通过 |
| B07 | 跨会话／工作区工具与摘要 API 隔离负例通过 |
| B08 | 确定性取消／时限／并发／保存后取消及真实摘要停止通过 |
| B09 | 旧限额跨越、独立超时、摘要额度、三次重试与一次溢出恢复边界通过 |
| B10 | 原生 2,000 字符截短与完整工具原文、写效果不重放；真实长资料读取后续作通过 |
| B11 | 正常独立进程恢复、未完成历史、无效摘要、伪造归属及写失败污染处理通过 |
| B12 | 27 项浏览器回归与真实桌面／手机页面检查通过 |
| B13 | 类型、lint、依赖边界、确定性测试、E2E 与构建通过 |

## 过程问题与处理

1. 首次真实调用返回 HTTP 402；最小 16-token 输出诊断同样失败，排除新输出能力参数导致。用户充值后继续通过，失败未计为验收成功。
2. 初次停止样本仅有一轮长历史，Pi 没有可摘要前缀，未触发压缩；增加第二轮历史后实际进入摘要并成功停止。
3. 独立审查修正空闲计时为 HTTP 字节进展（包括 SSE 心跳），补上跨请求伪造摘要关联拒绝、工具超时等待收敛以及摘要保存后立即取消的记录保留。
4. 网页探针补齐异步服务创建的 await 后重跑成功；测试脚本问题未记为产品通过证据。

开发前确认无活动请求，停服备份全部原数据到 `experiments/harness-lab/.backups/w01-3-20260917-185345`。`.env.local` 未改写，真实验收均使用独立目录。交付时恢复 5173／4310 开发服务，3 个原有会话无活动／恢复警告，原生文件与备份逐字节一致。

本期不承诺无损摘要或所有模型兼容；特殊项目数据管理、历史检索、子 Agent、正式 Run／崩溃恢复和外部调度仍按后续阶段设计。
