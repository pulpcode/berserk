# 验收记录（2026-09-17）

本次四项要求均完成：全局新建先选项目、分组加号直接新建、仅未读回复显示蓝点、模型配置设置窗口。项目沿用工作区存储与隔离，不迁移历史。

## 验证

- `npm test`：8 个测试文件、56 项通过；包含真实 Pi 适配器配合模拟 HTTP 的配置切换、重启、密钥不泄露与写入互斥测试。
- `npm run test:e2e -- --workers=2`：22 项通过。覆盖已有对话、草稿、流式取消、资料编辑、导航与阅读位置；新增项目选择/取消、折叠加号、延迟创建后的导航、跨项目创建失败可见、手机新建聚焦、设置保存/冲突/失败/忙碌、API Key 关闭清除与浏览器存储不泄露、设置覆盖时不误标已读。
- `npm run typecheck`、`npm run lint`、`npm run build`、`git diff --check`：通过。
- Trellis implement/check 清单校验通过；独立审阅完成。审阅发现的跨项目创建失败提示、全部动态导航竞争、手机新建焦点均已修复并由浏览器测试覆盖。
- 实际启动本地服务，在 Chrome 查看 1440×960 主页面、项目选择窗口、设置窗口及 375×812 手机设置窗口：无页面异常、无横向溢出、字段和保存按钮可见。API Key 初始为空。

视觉检查图片位于当前机器临时目录：`/tmp/berserk-project-desktop.png`、`/tmp/berserk-project-picker.png`、`/tmp/berserk-project-settings.png`、`/tmp/berserk-project-settings-mobile.png`；临时图片不作为仓库长期依赖。

## 范围说明

标准测试使用隔离目录和模拟服务，没有发送真实 LLM 请求。本地视觉检查仅打开窗口和读取现有配置，没有保存用户配置或新建用户会话。现有 `.env.local` 和用户工作区数据未迁移。

本期仅提供单个全局模型配置，兼容现有 OpenAI Chat Completions 通道。服务商标识/模型 ID/HTTPS API 地址/API Key 可保存；没有增加模型目录发现、多模型路由或其他空设置分类。模型配置保存成功并不代表验证了远端账号权限，实际连接结果由下一次对话返回。
