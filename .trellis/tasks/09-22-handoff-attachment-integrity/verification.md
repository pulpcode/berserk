# 实施与验收记录

2026-09-22。已完成已审范围内的代码修改；**自然多轮分派验收未通过，任务暂不收口。** 现已按用户要求更新线上服务供人工测试，未修改原测试工作。

原附件遗漏及后续线上测试发现的命令确认、跨轮分派问题，统一跟踪于 [三项缺陷记录](bugs.md)。该记录不代表新增修复已获批准或验收通过。

## 已实现

- 工作区说明明确同任务、同席位共享文件，不同席位目录独立。
- 分派目标、附件路径及导入编号补充字段说明；准备工具说明明确参数与 prepare → commit 的衔接。参数仍可选，执行逻辑和数据格式不变。
- 两类分派确认在工作说明前显示实际附件数量、文件和预览；零附件明确可见。B 的输入资料为空时也显示。复用原固定副本，不增加复制或确认步骤。
- 增加自然多轮及对照探针，保留真实请求、工具参数、固定文件与导入字节证据。

实施前提交：`ef25acf`，`记录跨席位文件交接修复方案与测试资料`。本轮实现暂留工作区，待验收问题处理后收口。

## 真实模型与 Docker 验证

使用腾讯云独立目录 `/home/ubuntu/axon-handoff-validation.pBhEnB`、DeepSeek `deepseek-flash`、Pi 0.85.1 与已有 Docker 镜像。探针自行创建临时数据，不打开线上数据，也不启动第二套线上服务。

| 场景 | 结果 |
| --- | --- |
| 修改前自然多轮基线 | 已读取两份文稿；模型额外询问任务范围，探针取消。未执行分派，未复现原漏附件。 |
| 修改后自然多轮 1 | 额外询问是否按上述范围准备、是否带无关文件；取消，未交接。 |
| 修改后自然多轮 2 | 两个 `inputPaths` 正确，但模型填入虚构 `taskSpaceId`，准备被拒绝；随后询问项目 ID 和范围，取消。 |
| 修改后自然多轮 3 | 再次询问工作范围与是否附上两份资料；取消，未交接。 |
| 只交一份文件 | 通过。实际仅交指定初稿，无关文件未交接；B 通过工具导入，字节及 hash 一致。 |
| 纯文字分派 | 模型询问项目 ID 和标题，取消；没有走到分派，不能据此判断模型已完成纯文字验收。原接口允许零附件，页面用例已通过。 |
| 明确双附件与成果回传 | 通过。A 交两份文件 → B 导入、修订、正式提交 → A 导入成果；文件字节、hash 一致，跨席位目录隔离。 |

基线记录 5 次模型请求，修改后六组场景共 46 次。各场景只执行一次，失败未重跑。探针对固定目标出现额外问题时主动取消，表中“取消”是测试处理方式，不是模型或产品自行报错。

已核对实际 HTTP 模型请求：正确席位、当前项目 ID、新工作区说明、附件字段说明、提交版本号说明和成果导入编号说明均存在。模型填错或声称缺少项目 ID，不能归因于本次输入没有提供该 ID；是否存在术语映射歧义或其他模型使用问题，仍需进一步验证。

验收对应：A1、A4、A5、A6 已有验证支持；A2 未通过；A3 单附件部分通过，纯文字真实模型部分未完成。明确双附件成功不能替代 A2。

证据索引及原始文件校验值见 [implementation-verification.json](research/implementation-verification.json)。服务器原始证据：`/tmp/axon-handoff-live-AHthix`、`/tmp/axon-handoff-live-HDB4Gs`；本地副本：`/tmp/axon-handoff-review/`。均为合成测试资料，不含请求头或凭证。

## 代码与界面检查

| 检查 | 结果 |
| --- | --- |
| TypeScript、Lint、依赖边界、构建、diff 格式 | 通过；构建仍有既有大包提示。 |
| 受影响的交接服务与 Pi 测试 | 26 项通过。 |
| 全量浏览器回归 | 98 项通过。新增截图文案校正后，5 项附件用例再次通过。 |
| 全量单元／集成测试 | 462 项中 461 项通过；后台投递的一项异步断言失败。单独运行该项通过，尚不能记为全量绿灯。 |
| 独立代码审查 | 未发现本次实现的代码缺陷；要求保留自然多轮未通过结论。 |

首次 6 路浏览器回归中，既有席位切换用例 30 秒超时；单独运行及最终 2 路全量回归均通过，未修改该用例逻辑。

后台测试 `tests/background/service.test.ts:264` 在观察到 B 投递失败后立即断言 A 已投递。实现按记录倒序处理，B 失败后 A 仍可能正在异步核验，因此该断言没有等待 A 自己的终态。修改前提交的该项独立测试连续 4 次通过；修改后全套与该文件测试失败、该项独立测试通过，符合时序敏感表现，不能声称已在旧提交复现失败。本次未修改后台产品代码或扩大修复范围。

界面截图：[零附件](research/confirmation-0.png)、[两份附件及预览](research/confirmation-2.png)。截图来自隔离浏览器测试。

## 待审的最小后续处理

现有证据没有证明必须强制填写 `inputPaths`。先针对已出现的问题讨论以下补充，尚未实现：

1. 明确宿主“当前项目”与工具 `taskSpaceId` 的字段映射，避免要求用户提供系统已知的 ID；是否进一步由宿主固定该参数，单独审阅后决定。
2. 核对现有澄清说明与准备确认的职责是否重叠，以实际不明确的对象为澄清条件，避免对已明确目标重复询问。任何说明调整继续保留失败样本验证，不保证靠一句提示解决。
3. 后台测试单独修正等待目标席位终态的断言，不改变投递顺序或新增产品机制。

本次未新增参数必填、意图检查、重复读文件、额外确认或自动补附件。旧坏单仍保留。

## 线上更新

2026-09-22 已按用户要求部署当前实现至腾讯云原 4315 单实例，入口仍为 http://127.0.0.1:4315/ 。发布包括六个说明／界面源文件及探针脚本，不包含后续建议中的项目 ID 自动带入。源码来自 `ef25acf` 加当前未提交实现，归档 hash 与实际变更见 [发布记录](research/deployment.json)。

更新前前台请求、后台作业和待投递均为空。停服后备份并校验 52 个数据文件及原程序，再替换代码并重启；仍使用原数据目录。重启后 A/B 登录、会话 ID、工作待办数和信息收件数一致，51 个非 SQLite 内容文件 hash 不变；健康检查、新版 JS 实际响应及本地 SSH 转发入口均正常。进程仅监听原 Axon 4315 端口，4316 未启动。

自然多轮验收及后台测试时序问题仍按以上记录保留，本次发布不将其标记为已修复。
