# 实施与验证计划

状态：**待审，审批后执行**。本轮只修改方案，不修改产品代码、运行模型验证或操作线上业务数据。

| 步骤 | 内容与退出条件 |
| --- | --- |
| P1 复现基线 | 在隔离数据中补充原自然多轮场景，保留修改前的实际参数与结果。使用现有 `scripts/probe-handoff.ts`，增加无关文件对照；不向模型提示内部字段，不要求必须复现失败才继续。 |
| P2 补清说明 | 按设计第 2、3 节替换 `src/pi/lab.ts` 的工作区说明、`src/pi/collaboration-tools.ts` 的准备工具说明，并在 `src/contracts/collaboration.ts` 补充目标、附件路径和导入文件编号的描述。核对实际模型输入，明确 submit 同样需要版本号、导入支持输入资料及提交成果；保持参数可选性与现有执行逻辑。 |
| P3 验证效果 | 运行三次独立自然多轮场景，检查真实附件及 B 导入字节；补充只交一份、纯文字分派和明确两份附件对照。若仍失败，留存证据并提出补充方案，不自行加入必填 schema、参数拦截或更多校验流程。 |
| P4 实际附件展示 | 修改 `src/web/InteractionCard.tsx`、`WorkInbox.tsx`，复用 `HandoffFiles.tsx` 展示实际数量、文件和零附件状态。保留现有确认操作，区分分派与其他业务操作。 |
| P5 回归与收口 | 覆盖真实附件展示、旧记录回读、现有权限和失败处理；同步相关规范与验收记录。明确验证结果和剩余模型风险，再安排服务更新及新的人工测试；旧测试单保留。 |

P3 是是否扩展修复范围的判断点。增加参数必填或新的宿主校验，需要先提供失败依据和具体方案供用户审阅，不属于本方案自动执行的后续步骤。

## 验证安排

- 实际模型输入核对：通过探针记录检查共享范围、字段含义和调用衔接是否完整；不新增逐字提示词快照测试。
- `tests/pi/collaboration.test.ts`：沿用现有 Pi 循环和交接测试，确认正常流程没有增加工具查询或人工确认步骤；不引入“漏字段必报错”的预期。
- `tests/collaboration/`：复用合法纯文字分派、路径权限、固定副本和旧记录检查；只为发现的覆盖缺口补用例。
- `tests/e2e/handoff.spec.ts`：覆盖聊天与页面确认的附件数量、预览和空状态；说明写着“随附两份”但实际为空时如实展示；B 输入资料为空可见；其他操作不出现分派附件误报。
- `scripts/probe-handoff.ts`：保留每次真实模型输入、工具参数、准备结果和交接后字节核对结果；用不同文件名及任务要求作对照，覆盖 B 提交后 A 导入成果。模拟模型通过不能代替自然多轮验证。

开工前加载 `trellis-before-dev`。`frontend/harness-lab.md` 超过单文件自动注入上限，实施和检查时须分段读取全文。UI 延续现有桌面界面。

在 `experiments/harness-lab` 中先跑受影响测试，收口按仓库要求完成检查：

```bash
npm run typecheck
npm run lint
npm test -- --maxWorkers=2
npm run test:e2e
npm run build
git diff --check
```

真实模型和 Docker 验证使用隔离测试目录，兼容验证使用数据副本，不修改线上测试工作。本次不新增数据格式，回退不需要转换历史记录。

使用现有模型和服务器接入条件，不需要新增 API key 或密码。提交信息继续以中文为主体。
