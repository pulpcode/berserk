# 实施与验证记录

日期：2026-09-23。本期实现已按用户授权更新至腾讯云 4315 单实例；未提交 Git。

当前结论：代码实现、本地验证及发布检查完成；用户已报告一轮端到端流程通过，服务端回执核对为分派、签收、提交、验收完成。A9 已有单轮手动验证支持；未完成多轮或完整自动真实模型探针，不把本次结果扩大为长期稳定性保证。

## 已实现

- 前后台共用命令策略 v2：支持同目录字面量 `cd` 和四类文件检查命令的简单文件名通配；保留整条命令检查、待审和禁止规则。
- 后台待审调用不执行，返回 Pi 原生工具错误，允许继续；普通对话确认及基础设施故障终止机制保留。
- Bash 模式说明进入实际模型工具描述。既有自定义系统提示不会自动采用 Pi 工具工厂的 `promptGuidelines`，因此只修改该字段不足以影响模型输入。
- 从已有原生记录投影命令、规则原因和实际执行情况；按本次请求展示详情，不新增日志或状态表。
- 信息处理中心保留侧边栏，提供四列看板、列表及详情；计数与分页在服务端完成，执行与投递按作业分别显示。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| 全量 Vitest，`npm test -- --maxWorkers=2` | 42 个文件、527 项通过 |
| TypeScript | 最终页面及验收脚本调整后通过 |
| ESLint 与依赖边界 | 最终检查通过 |
| Vite 构建 | 通过；保留现有大包体积提示 |
| 浏览器回归与视觉检查 | 全量 103 项通过（含信息中心 9 项）；1440／1100 桌面截图通过 |
| 独立代码审查 | 运行、查询、页面及验收脚本复核通过，无剩余代码阻断项 |
| Git diff 格式检查 | 通过 |

关键断言覆盖两条复现命令、后续危险命令检查、前台确认、两种后台模式受阻后继续、正常无法完成答复仍投递、历史记录与重启、多状态合并分页、来源授权、各作业的投递归属及旧作业不混入后续会话。

浏览器检查另覆盖作业数量减少后的返回分页、关联会话打开失败的可见反馈及迟到错误隔离。截图：[作业看板](research/information-board-desktop.png)、[执行详情](research/information-drawer-desktop.png)、[其他席位最小状态摘要](research/information-center-desktop.png)。

## 真实环境验收

本机 Docker 未启动。已只读确认 `tencent-server` 的 Docker 26.1.3 与既有文件运行镜像可用；创建独立目录 `/home/ubuntu/axon-board-validation.Q2ytnA`。

此前隔离探针因上传审批未运行。用户随后明确批准先更新线上服务，由其手动测试；已完成下述发布检查，没有运行自动真实模型探针，不能把发布成功视为 A9 通过。

隔离验收入口：

- `npm run probe:background -- --feedback`：核对指定 Bash 调用的 `ask / not_started` 记录、实际工具错误、随后读取及文件保留。
- `npm run probe:background`：消息接入、真实 Python 处理、双席位投递、对话及后台分析、A 分派两份附件、B 签收修订并提交 A。

探针只操作临时账号、任务和合成文件。交接确认限定本次指定工作，不自动批准 shell。B 的验收核对当前请求中每份交接附件的导入、导入后成功读取及文件 hash，避免把 B 已有资料误算成交接成功。每次执行保留 `validation.json`；探针 600 秒时限仅用于验收，不改变产品默认配置。

## 线上发布

用户授权：“批准更新线上服务，我来测试”。发布至既有腾讯云 4315 单实例，没有启动 4316。

- 源码包包含 183 个代码、构建配置、静态资源及合成测试文件，不含本地密钥或业务数据；服务器依赖锁与本地一致，暂存构建通过。
- 发布前确认两个席位全部 7 个会话无活动请求，后台没有排队或执行中作业。
- 停服后完整备份并校验 111 个数据文件，同时备份旧代码和服务器配置；保留原数据目录，重启后 110 个非 SQLite 内容文件 hash 不变。
- 两个账号登录、7 个原会话读取、11 条后台作业详情、四列状态查询、前端入口及新 JS 均通过检查。
- Logo、字标及 favicon 的实际 HTTP 内容与本地资源一致；用户使用的本机 4315 转发可访问。
- 在服务器上调用命令策略函数核对两条复现命令为 allow，后接 `rm` 为 ask、后接 `sudo` 为 deny；没有实际执行这些检查命令或调用模型。

备份目录：`/home/ubuntu/axon-release-background.ClBNID/updates/background-board-v0ueqoob/backup`。发布指纹及检查结果见 [发布记录](research/deployment.json)。未修改或自动重跑历史失败作业；用户可刷新页面后发起新的测试或明确重新预处理。

## 保留边界

2026-09-23 按用户要求发送一条新的“值班交接说明”复测信息，经来源 API 上传初稿和修订要求、接入消息。真实预处理状态为 `succeeded`，已投递席位 A；未重试旧作业或代用户操作业务确认。记录见 [复测04](research/manual-retest-04.json)。用户随后报告完成一轮端到端测试；只读核对工作为 `completed`，含两份输入附件、一份成果及四项 committed 回执，详见 [Issue 审阅与业务证据](research/issue-review.md)。

没有新增后台审批等待、自动转前台、接手投递或权限体系。日志详情展示已保存工具输出及 `/logs` 引用，没有新增下载接口。后续权限完善见 [PERM-001](issues.md)。
