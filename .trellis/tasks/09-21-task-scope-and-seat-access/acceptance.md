# 验收：业务任务、可见范围与席位入口

状态：**待审，尚未执行**。按 D01～D03 的建议设计；确认产品选择后再执行，不与后续后台作业验收绑定。

| 编号 | 场景与通过标准 | 需求 |
| --- | --- | --- |
| TA01 | A、B 以独立浏览器身份登录，分别进入固定席位；工作台无任意身份切换器。未登录、伪造 seatId 或旧测试路径不能读写业务数据，正式模式不回退测试身份 | TS01、TS10 |
| TA02 | 登录成功更换会话标识，Cookie 属性、到期、退出及 CSRF／Origin 校验生效；重置或停用后旧登录失效。统一错误、限流及秘密不回显经过检查 | TS01、TS08 |
| TA03 | A 有公共创建权限，明确确认后建立任务，B 能看任务说明；B 无该权限时不能通过接口创建。创建能力不授予其他任务管理或私人内容访问权 | TS02、TS10 |
| TA04 | A 建立私有任务，B 在列表、搜索、直接 ID、文件下载、资源、日志、Agents 和工作项入口均不可越权读取；私有任务不出现在公共目录 | TS03、TS10 |
| TA05 | B 查看公共任务不复制 A 的数据；新建会话时只准备自己的工作区，两个席位的 AGENTS.md、上传文件、会话标题及工具上下文互不串用 | TS04、TS10 |
| TA06 | 公共任务中完成原分派、签收、提交、退回及再验收，输入／成果固定副本仍可查；第三个测试身份仅能看任务说明，不能读两方交接。私有任务跨席位分派被拒绝 | TS05、TS10 |
| TA07 | 归档前有活动处理、在途写、待确认或未完成工作项时明确拒绝；满足条件才归档。归档后历史可读而新写／新请求被拒绝；重新开启可继续；归档与新工作竞争只有一方有效推进 | TS06、TS10 |
| TA08 | 重复创建请求返回同一任务；同 clientActionId 异内容冲突。任务编辑校验 revision；名称与目标读取一致，旧页保存冲突保留草稿，不静默覆盖 | TS02、TS03、TS07 |
| TA09 | 全局／任务行新建对话正确绑定任务；公共与私有分组、无权创建、空列表、归档筛选可理解。切换任务保留草稿、焦点、滚动、未读及真实进度；只验收桌面端 | TS07 |
| TA10 | 退出／过期后受保护流关闭、页面数据清除，不能继续下载或操作；已接受 Agent 请求保持原归属继续处理，重新登录读回结果不重跑。HITL 必须重新认证为有权身份后操作 | TS08、TS10 |
| TA11 | 同浏览器 A 标签退出并改登 B，旧标签不能把 A 的草稿、上传或确认以 B 身份提交；迟到回复不写进 B 页面；不同账号草稿／附件不串用，退出前说明未发送内容会清除 | TS07、TS08、TS10 |
| TA12 | 共享模型设置只允许有管理能力的席位修改；普通席位不可替换共享密钥。账号密码、会话标识和签名密钥不进入 Pi 提示、沙盒、文件输出或日志 | TS01、TS10 |
| TA13 | 旧单席位项目按确认映射私有，多席位／交接项目逐项明确公共映射与负责人；旧任务／工作区／会话／操作 ID、原始文件和 Pi JSONL 字节保持，不把旧数据自动归第一个账号 | TS09 |
| TA14 | 缺映射、任务名冲突、损坏数据或迁移中断时不开放不完整业务；迁移可重入且不重复建任务。任务已创建但目录准备失败可重试，同一任务不因此重复；备份回退经过验证 | TS09 |
| TA15 | A、B 在同一公共任务分别运行、取消和读写各自文件，互不改变身份或中止对方请求；任务元数据短锁不把整项对话执行串行化 | TS04、TS10 |
| TA16 | 回归多轮对话、原生历史／压缩、文件上传下载／引用、Skill／Agents、HITL、会话恢复和双席位交接；正常路径另记录真实模型／执行环境验证，不把确定性测试称为真实验收 | TS01～TS10 |

测试中第三身份仅用于证明“公共任务可见不等于业务交接可见”，不要求建设成员管理。业务身份与测试模式必须分别验证，不能只用无认证测试接口证明正式访问限制。

故障测试使用隔离数据和真实存储，核对迁移前后文件摘要及业务回执；文档检查、代码测试、真实 API／Docker、浏览器验收和部署结果分别记录。没有执行的项不得标为通过。
