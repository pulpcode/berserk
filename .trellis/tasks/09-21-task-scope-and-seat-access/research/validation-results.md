# 实施验证记录

日期：2026-09-21。设计基线提交：`a4080ea`。本期实现留在工作区，待用户验收；未 push、未更新线上服务。

## 已实现

- 最小账号登录、固定席位、会话过期与退出、接口权限和模型设置管理能力。
- 公共／席位私有任务目录，名称与目标、创建去重、编辑冲突、归档及重新开启。
- 查看公共任务只读取说明；开始工作才准备本席位目录。会话、文件和交接参与方权限继续独立。
- 桌面任务分组与说明面板；退出后清除未发送内容，旧账号请求与另一账号页面隔离。
- 离线开通／停用账号、完整备份后初始化新数据；不实现复杂旧项目迁移。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| TypeScript、ESLint、模块边界 | 通过 |
| Vitest 全量 | 34 文件、402 项通过；覆盖原生历史／压缩、文件、交接、HITL 等回归 |
| 新增账号／任务测试 | 7 项通过；真实 SQLite 与 Pi 会话、确定性模型，含权限、归档、会话失效、重启及备份 |
| Playwright 全量 | 87 项通过；后续复测发现跨标签页登录竞争，修复后相关 2 场景各重复 3 次，6 次全部通过 |
| 前端生产构建 | 通过；仍有单包超过 500 KiB 的构建提示 |
| 桌面视觉检查 | 检查公共／私有目录和账号入口，修复底部账号与设置拥挤换行 |
| 真实模型与 Linux Docker | 独立数据运行 3 轮，全部成功，见下文 |
| Git 差异空白检查 | 通过 |

浏览器登录测试经过真正的账号／任务服务端处理，模型回复使用确定性运行时。不能据此称为浏览器真实 LLM 验收。

跨标签页问题来自退出后两个标签页各自创建匿名会话，Cookie 与 CSRF 可能错配。现在退出先撤销登录并生成同一个匿名会话，再通知其他标签；仍复用 Fastify 原生会话机制。新增测试也核对匿名标签读取相同 CSRF、旧登录不能访问业务数据。

## 真实模型与沙盒

使用 `tencent-server` 的独立验证目录 `/home/ubuntu/axon-access-validation.8Zmyov`，没有替换线上程序、监听端口或生产数据。脱敏结果保存于 [live-access.json](live-access.json)。

1. A 在公共任务中根据任务目标生成并读取 `report-a.txt`，调用 `file_output`。
2. B 在同一公共任务自己的目录生成 `report-b.txt`，调用 `file_output`；验证 B 目录没有 A 的文件。
3. 关闭并重新打开 PiLab，A 在原会话读取原文件并继续回复。

合计 12 次模型尝试、10 次工具调用。真实验证覆盖任务上下文、文件及重启正常路径；登录与越权以确定性测试为主要证据。本次未重新对所有 HITL、交接、压缩分支执行付费模型调用。

第一次远程打包带入 macOS `._` 旁文件，被角色配置校验拒绝；改用不含扩展属性的打包方式后通过。独立目录保留初次失败日志。最后的退出同步和元数据校验修复由本地测试验证，未因此重复付费探针。

## 历史数据与发布

备份初始化只对测试目录执行：旧 JSONL 原字节保留、模型设置沿用、备份重复目标拒绝覆盖。没有删除用户历史。

本次尚未进行正式服务器账号开通、生产数据切换或线上浏览器验收。更新服务时需要停服备份，再由维护命令输入账号密码并生成本地签名配置；不在对话或 Git 中保存密码。操作入口见实验目录 README 的“业务任务与账号入口”。

## 线上更新（用户随后授权）

已发布到 `tencent-server` 的唯一 `127.0.0.1:4315` 实例，发布目录 `/home/ubuntu/axon-release-access.QCcDaB`，PID 1154813。4316 未启动。使用本轮工作区代码构建，尚未新增 Git 提交或 push。

发布前确认两席位无活动请求；停服后将原数据完整复制到发布目录 `backups/4315`，51 个文件 SHA-256 全部一致。原数据根保持不变；新版使用发布目录 `data`，仅继承模型设置，没有导入旧任务与会话。旧程序与启动脚本保留可回退。

创建 operator-a／operator-b 两个随机密码测试账号，对应席位 A／B。密码仅保存于受限本地文件 `.local/access-release/test-accounts.md` 与服务器发布目录的受限文件，不纳入 Git。A 可创建公共任务、管理模型；B 可参与公共任务及创建席位私有任务。

线上 HTTP 验证两账号登录、固定席位、空任务目录、DeepSeek Flash 配置与上下文参数就绪、退出及匿名业务访问 401；浏览器验证新版登录页可见。未产生测试业务任务、未额外调用真实模型。用户可使用普通和隐身窗口同时验证两席位。

### 测试账号简化

按用户要求将线上 operator-a／operator-b 改名为 a／b，并重设为用户指定的测试密码；不在此记录密码。保留账号 ID、席位、权限和全部业务数据，撤销旧登录。操作前确认无活动请求，完整备份至 `backups/login-change-1789992197`；新账号在线登录及席位归属验证通过，本地受限账号说明已同步。仅调整这两个测试账号，不修改通用账号开通密码策略。

### 正式登录后 Agent 缺少席位清单

检查线上席位 A「测试任务」会话发现，模型上下文仍使用 `config.testSeats`，正式登录禁用该配置后输出 `undefined`；模型连续使用 work_item_list 查询待办，无法发现账号目录中的席位 B。数据库中 B 处于启用状态，公共任务归属正常。未实际调用分派 prepare／commit。

本地修复为每请求从 AccessStore 获取启用席位 id/name，显式测试模式才回退 testSeats。新增回归检查正式上下文包含 B、排除停用 C，以及同会话下一轮移除已停用 B。typecheck、lint 及 access／Pi collaboration 两文件15项测试通过。

修复文件已上传为发布目录 `lab-next.ts`，尚未覆盖线上代码。自动审批拒绝停服部署，理由为本轮用户仅授权检查、尚未明确授权线上更新。等待用户确认；没有执行被拒绝脚本或采用其他方式绕过。用户原会话与业务数据未改动。

### 席位清单修复已发布

用户随后明确授权更新服务。已确认无活动请求，完整备份至 `backups/seat-context-1789993393`，同步修复并重启唯一4315实例（PID 1162860）。线上源码SHA-256与本地修复一致；原Pi JSONL及workspace-index逐字节核对一致；健康检查正常，启用席位返回A与B。未新增工具、未改变工具描述，等待用户在原会话重新测试。

### 原会话仍重复旧席位结论：离线排查

12:24、12:25 UTC 回复发生在修复发布后，仍声称清单undefined。使用发布前原会话备份副本离线重建：确定性Pi调用及真实提供方序列化（拦截全部fetch，未发送网络）均包含启用A/B的id和name；旧助手回答中的undefined同时保留在历史。证据位于远端 `/tmp/axon-seat-offline-qrqT3O`、`/tmp/axon-seat-offline-k86nFQ`。这支持历史错误影响模型判断的假设，但尚未通过真实模型对照确定成因；不能将模型自述当作实际请求体缺字段的证据。

真实模型重放因自动审批要求用户明确授权将该历史再次发送DeepSeek而未执行。线上代码、原会话未进一步改动；没有添加提示补丁或查询工具。

### 用户批准后的真实 DeepSeek 对照

用户明确批准将测试会话副本发送给当前 DeepSeek。使用线上代码和发布前固定备份，各建立2个原历史副本、2个无历史新会话；同一任务和席位、同一问题“重新看一下，现在有哪些可选的席位？”。全部使用 deepseek-flash，thinking关闭；没有修改提示或工具描述。系统消息与工具定义哈希在四组之间完全一致。原历史取自12:23发布前备份，包含最初多次undefined错误；不含用户12:24、12:25的后续两轮。探针在独立目录运行，禁止准备及提交交接，未写入原会话。

| 组别 | 结果 |
| --- | --- |
| 原历史1 | 直接正确列出A/B、seat-b，并纠正旧回答；无工具调用 |
| 无历史1 | 正确列出A/B、seat-b；无工具调用 |
| 原历史2 | 调用work_item_list，先说没有可选席位，随后明确纠正并列出A/B、seat-b；回答前后矛盾 |
| 无历史2 | 正确列出A/B、seat-b；无工具调用 |

共4次用户请求、5次模型调用、1次只读工具调用；无分派、无压缩。结果保存于 [seat-context-comparison.json](seat-context-comparison.json)，远端 `/tmp/axon-seat-comparison-O16pSl/results.json`。

结论：修复后的清单已真正进入请求且模型能读取；带旧历史时出现了一次延续旧结论、随后自我纠正的现象，支持历史影响的假设。但两次旧历史测试最终都识别B，没有稳定复现截图中的完全失败，不足以认定历史是唯一原因或问题已彻底解决。保持线上代码不变，不据此新增工具、提示补丁或清理历史。
