# W01-7 验收清单

状态：**G01～G15 组合验收通过，待用户验收**。证据对应见 [验收记录](research/validation-results.md)，包含实际 Axon／Pi、浏览器与真实 Docker／模型验证。

| 编号 | 场景与通过标准 | 需求 |
| --- | --- | --- |
| G01 | 已完成与空会话重启后行为不变；普通中断仅根据原记录判断，JSONL 字节不变；显式新消息使用新 requestId、原 sessionId 和工作区 | J01、J02、J04 |
| G02 | 首次对话在用户消息保存前／后、等待回复、流式输出中强制退出；已保存内容保留，未保存片段不虚构；无请求证据的空会话不编造中断请求 | J01、J02、J08 |
| G03 | 分别在工具执行前、实际写文件后但结果保存前、真实结果保存后退出；效果分别为 0、1、1，重新打开不会增加效果；缺结果与真实结果展示准确 | J03、J04 |
| G04 | Pi 发给模型的缺失结果补位没有写进原历史；显式续聊不重复原用户消息。让模型另发同操作的新调用可产生第二次效果，证据不宣称自动去重 | J03、J04、J06 |
| G05 | 连续重启、GET、刷新均不改写历史、不调用模型或工具；A 中断后 B 完成、A／B 连续中断均能按已有请求边界区分；正常终态已存而 SSE 未送达不误判 | J02、J04 |
| G06 | 坏 JSONL、错误归属、非法或缺失请求关联、原有持久化失败均保留保护；不得截尾、删除或补造记录；合法的多个历史中断不因缺结束记录一律被拒绝 | J07 |
| G07 | 等待回答时退出，旧卡片失效；答案已存但工具未返回时保留原答案，不伪造 toolResult；旧提交被拒绝，新请求能正常提问和接收回答 | J05 |
| G08 | 待批准、已拒绝、已批准缺结果分别重启；旧批准不再授权，批准缺结果显示未知；新请求同类受控操作必须重新确认，重复旧提交不执行 | J03、J05、J06 |
| G09 | 真实 Docker 命令已写文件且仍在运行时退出宿主；沿用现有初始化清理，完成后放行并保留文件；初始化失败时相关中断会话仍受保护，不回退宿主执行 | J01、J03、J07 |
| G10 | 压缩调用中、摘要保存后两处退出；只用有效 CompactionEntry 续聊，原文保留、不伪造摘要；新请求仍可按 Pi 原生规则压缩并使用当前指令 | J01、J06 |
| G11 | 未结束子任务、子文件缺失、父子结果不一致保持阻止；子任务已正常完成而父回复中断可续聊；不重跑旧子任务或读取其他会话的子历史 | J06、J07 |
| G12 | 续聊使用当前 AGENTS.md、原工具范围和 Pi 原生上下文／压缩；不新增恢复提示词、摘要或历史条目；后续成功不将旧交互的 unknown 改为成功 | J03、J06 |
| G13 | 浏览器显示最近请求“已中断”、必要的缺失结果说明和过期卡片；可发送但不自动发送。草稿、附件及本地备份行为保留，落盘消息不填回，不覆盖正在编辑的内容 | J02、J05、J08 |
| G14 | 两个会话分别中断／正常运行，状态、草稿、附件、停止和新交互不串线；普通断网重新连接仍是原活动请求，不误判崩溃；蓝点仍只对应未读回复 | J04、J06、J08 |
| G15 | 经真实 DeepSeek 和独立 Ubuntu／Docker 完成“已有历史中断→新消息检查文件→继续处理”、过期提问后新提问、旧批准后新确认；核对调用、记录与文件实际效果，质量检查及必要全量回归通过 | J01～J08 |

G02、G03、G07～G11 须覆盖实际 Axon／Pi 路径；核心崩溃位置用测试子进程 SIGKILL，不以用户停止、网络断开或优雅重启替代。故障注入与确定性模型证明机制；真实模型验证续聊语义，两类证据分别记录。

所有文件效果测试只操作隔离工作区的测试文件。旧历史、备份、JSONL、实际请求数及容器清理结果构成证据；不以模型自述“已经恢复”判定通过。
