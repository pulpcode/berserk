# W09-A 验收清单

状态：**待审；以下为验收要求，尚未执行**。涉及任务目录、资料权限与结果入口的用例，按 PRD 待审建议编写，实施前确认。

| 编号 | 场景与通过标准 | 需求 |
| --- | --- | --- |
| BG01 | 关闭全部浏览器后接入信息；确认接收后重启，原文、固定文件和待处理记录仍在。正文／文件未保存成功不返回已接收；服务停机时由来源重送未确认消息 | R01、R08 |
| BG02 | 同来源消息重送只得到原事件和一次处理／送达；重传附件不因临时 ID 改变而误冲突。同键异文拒绝，同 subject 的新消息独立保留 | R02 |
| BG03 | 未确认／拒绝的业务动作不产生成功事件；业务、回执、事件同事务，重复 operationId 不再产生事件。work.assigned 关联已有工作，不重复分派或默认调用模型 | R02、R07 |
| BG04 | 已知目标直接执行，无需模型的送达不调用模型。需判断的输入可查询上下文并选择获准 Agents／接收方，不限固定 optionId；无目标、冲突配置和无效交付有可见处理入口，不静默改派 | R03、R11 |
| BG05 | 同一后台机制分别完成资料分析和脚本文件处理，后者真实生成并执行脚本、保留中间文件和可读交付物；均可不创建 WorkItem／Submission。只声称生成文件不能算完成 | R04 |
| BG06 | 输入不带任务 ID：能借获准目录关联一个或多个任务，也能明确无相关任务；空关联不判失败，多关联不自动启动多个作业。来源引用与 Agent 推断可区分 | R03、R05 |
| BG07 | 已有 Submission 触发检查后意见关联原提交；不新增 Submission、不通过／退回原工作。正式上报仍由人员经原有确认流程完成，自动生成文件不等于提交 | R04、R07 |
| BG08 | 人员查看结果不启动模型；选择资料、会话及工作区后形成可编辑草稿，发送才处理。无项目结果可查看，导入不覆盖同名异文；关联修正留痕且不改写原文或扩大可见范围 | R05、R07、R08 |
| BG09 | 缺资料时输出待核实问题并释放资源；需批准的命令未执行，尝试明确失败并提示接手。无长期 AskUser／确认等待，不把人工查看问题当验收；普通对话 HITL 不变 | R03、R07、R10、R11 |
| BG10 | 固定服务身份不借用席位；私有文件／会话、未开放任务及伪造对象 ID 被拒绝。模型、来源正文和任务关联均不能扩权；接收方无权接收已读资料时不能公开对应结果 | R11 |
| BG11 | Agent 能查询来源详情／历史及任务说明；实际读取的依据、时间、版本可回查。来源随后变化不改写旧依据；不可用、部分返回或资料不足明确说明，不解释为无影响，不要求完整来源副本 | R06、R11 |
| BG12 | 后台超容量持久化排队；前后台、摘要和父子调用共享模型限额。模型并发为 1 时父子仍可前进；等待／退避不占名额，排队可取消，不误触发流空闲超时 | R10 |
| BG13 | queued／running 取消先登记再停止，收尾后显示终态；取消与发布竞争有明确结果。已有文件／结果／回执保留，不承诺撤销已执行脚本 | R08 |
| BG14 | 在领取后首条 Pi 历史前、模型中、脚本执行后但结果未写时分别 SIGKILL。重启显示中断和可能未知的效果，保留目录／日志，不重放模型或脚本 | R08 |
| BG15 | 在原生正常结束、固定文件后及发布事务前后分别 SIGKILL。证据齐全只补发布一次，不重复结果／送达；证据缺失保持中断，不能补造成功历史 | R01、R02、R08 |
| BG16 | queued 重启后可领取，旧 running 先核对。人员重新处理用新尝试／目录并关联前次；变更目标或权限则新建关联作业，旧结果及确认边界保留，用量缺失标未知且父子不重复累计 | R08、R10 |
| BG17 | 同事项补充／乱序消息均保留，任务按需查询历史比较变化；结果各自固定依据，不按接收顺序判断业务真伪或覆盖旧分析。无全局同事项锁；自动产物不循环触发自身 | R02、R05、R06 |
| BG18 | 停用后新输入仍为 held；恢复不自动补跑历史 held。暂停领取保留队列而不取消在途；明确补处理、取消与重新处理可区分并幂等 | R01、R09 |
| BG19 | 只保存的结果不强制产生通知；指定席位收到获准结果，关联项目不自动公开给所有席位。关闭／刷新页面仍可见，查询及已读不增加模型调用、不改变业务状态 | R07、R09、R11 |
| BG20 | 工作台信息结果与正式工作待办分开，全部动态可查获准进度；管理台独立导航、列表和详情可用。切换保留草稿／会话／进度；无管理凭证不可访问，席位选择不授予管理权限 | R09、R11 |
| BG21 | 升级新增表及文件归属后，原 WorkItem／Submission／Receipt 可读写，旧文件及 Pi JSONL 字节不变；不强制迁移业务承办主体。损坏迁移报错，备份恢复及旧程序拒开新版库经过验证 | R08、R11 |
| BG22 | 独立数据以真实模型、查询工具和 Docker 完成关联分析、脚本文件处理、既有提交检查及一次委派，并回归对话、压缩、文件、HITL、恢复与双席位交接；逐例记录质量和真实边界 | R01～R11 |

质量样例至少包括：有任务引用、无任务引用影响 A／B 而非 C、无相关任务、目标明确的文件处理、缺关键资料、来源不可用、补充／乱序信息、多意图、冲突材料和伪造指令。预期关联及理由由验收样例明确，不提供给 Agent 作为答案。逐例对照目标、实际读取依据、结果和交付对象；不能用模型自报置信度或固定工具顺序代替质量判断。

故障验证使用独立进程、实际 SQLite／Pi JSONL／文件路径和命名中断点，核对持久化内容及真实调用数量，不只检查提示文字。测试、真实 API、浏览器和部署证据分别记录，未执行的项目不标为通过。
