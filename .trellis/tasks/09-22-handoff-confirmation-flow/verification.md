# 实施与验证

2026-09-22 完成实现与本地检查；2026-09-23 已按用户授权部署，等待用户真实测试。未提交 Git。

## 已实现

- 面向 Agent 只保留 `work_item_action` 交接入口，同一次调用完成准备、确认等待、批准后提交。
- 当前项目／工作区由宿主填入，模型仍提供业务对象、内容、附件和工作版本。
- 已知输入／业务错误在卡片前返回具体原因；内部故障继续停止。等待期间的变化由原提交校验处理。
- 沿用原卡片、固定文件与业务回执；网页 API 和存储不变。真实旧版 JSONL 夹具验证旧历史读取与续聊。
- 按用户决定，业务流程补充提示后置；本次只更新旧工具衔接说明。

## 验证

| 项目 | 结果 |
| --- | --- |
| TypeScript、Lint、依赖边界 | 通过 |
| 构建 | 通过；保留既有大包提示 |
| 全量单元／集成 | 482 项通过 |
| 独立代码审查 | 未发现生产代码缺陷；补齐提交前／提交后内部故障测试，定向 61 项通过 |
| 新增实际 Pi／API 网页确认测试 | 通过；批准前无工作，批准后一次回执，网页不另调 prepare／commit API |
| 浏览器回归 | 最终全量 99 项通过；首轮 97 通过、1 项身份切换超时，该组独立复测通过 |
| 真实模型与 Docker | 按用户要求先部署供其手动测试；尚未运行自动真实模型探针，不能据本地测试宣称自然语言验收完成 |

全量单测首轮复现父任务已记录的后台投递测试时序问题：B 的失败并不表示 A 已投递。仅将测试改为等待 A 自身的 delivered 结果，未改后台逻辑。自然多轮附件选择仍与 BUG-003 分开评估。

## 数据与发布

不修改线上旧测试单，不补造旧批准或工具结果。源码归档不含密钥、环境文件或线上数据。用户已明确批准具体发布包、腾讯云目标目录和结束旧确认等待，随后完成部署。

新历史已支持旧 prepare／commit 与新 action 两种格式。将来回退需保留此兼容解析，不能直接使用仅认识旧工具的二进制读取新记录；本轮通过代码归档发布，无新的 Git 发布或回退提交。

检查明细见 [结构化记录](research/implementation-verification.json)。

## 2026-09-23 线上更新

用户明确批准将源码发布包上传至现有腾讯云部署的独立更新目录，并结束 A 的旧确认等待。旧请求已取消，未批准或执行其中分派。

- 原 4315 单实例已更新，当前 PID `1695027`；未启动 4316。
- 停服后完整备份并校验 92 个数据文件，保留原数据目录；重启后 91 个非 SQLite 内容文件 hash 一致。
- A 的 5 个会话、4 条收件、2 项工作及 B 的 2 个会话、1 条收件、2 项工作均保留。
- 全部 7 个会话可读取，无 recoveryWarning；服务器健康检查、实际前端资源与本机转发入口均正常。
- 归档以 `047ab63` 加本轮工作区修改构建，未提交 Git；服务器构建通过。发布脚本的故障回退保留新历史解析及对应 schema，不回滚业务数据。

用户可刷新 http://127.0.0.1:4315/ 后在原会话重新发起交接；旧确认卡不再使用。未新建测试分派或自动调用模型。发布指纹、备份路径及取消记录见 [发布记录](research/deployment.json)。

## 发布静态资源遗漏修复

用户发现 Logo 显示失败。核对后确认本次源码归档遗漏 `public/`，使线上新 `dist/` 缺少图标、字标和 favicon；三个地址均返回 404。此前首页和 JS 检查通过不能证明这批资源已发布，这是发布打包及校验遗漏。

已从服务器原有 `public/` 原样补齐线上和暂存文件，无需重启、未改业务数据。通过用户实际使用的本地转发入口验证三个文件均返回 200，图片类型和字节 hash 与仓库一致。README 和 frontend spec 已补充 public 资源打包及逐项验证要求。详见 [修复记录](research/static-assets-repair.json)。

## 用户单轮端到端测试

2026-09-23 用户报告初步完成一轮端到端流程。只读核对复测04工作为 completed：Agent 分派两份附件、页面签收、Agent 提交一份成果、页面验收，四项操作均有 committed 回执。本地错误与取消回归已有覆盖，建议关闭 GitHub #3；不据这一轮宣称所有自然语言或全 Agent 路径均稳定。详见 [最新 Issue 审阅](../09-23-background-handoff-board/research/issue-review.md)。
