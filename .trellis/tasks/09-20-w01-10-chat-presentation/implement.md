# 实施与验证

1. 从已校验记录投影 turns 和 finalMessageId，验证成功、失败、中断、无最终正文、工具前说明、运行中及旧记录兼容。
2. 提取聊天渲染并接入紧凑过程分组，保护确认、错误、成果与顺序；保留现有焦点与滚动机制。
3. 针对分组/投影做有意义测试，运行类型、lint、既有浏览器回归和构建；检查桌面运行中、折叠后及展开截图。
4. 独立审查后更新现有测试服务，保留部署前完整备份。涉及只读API字段，前后端一起更新；旧记录无迁移。

不自动重发模型请求，不使用真实用户任务作为测试数据。改造前备份提交 c2ae7b2，Git信息以中文为主体。
