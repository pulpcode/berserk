# 验证记录

改造前备份提交：`c2ae7b2`（优化输入框与两级引用菜单，统一 Agents 界面命名）。本轮新改造于 2026-09-21 按用户要求提交并推送。

## 自动检查与审查

- 第一轮全量单元测试：33 个文件、393 项通过；新增边界补齐后，过程分组定向 12/12 通过。
- 完整浏览器回归首次 78/81；修正故障注入时序、既有确认文案断言和有效下载卡去重后，定向 9/9 通过。追加焦点边界回归 4/4 通过；无遗留失败。
- 最终类型检查、lint／依赖边界、构建、Git diff 检查通过。构建仍有既有主包大于 500 kB 的提示。
- 独立跨层审查通过：历史只读投影不改变 Pi 运行逻辑，不写新记录、不调用模型；确认／异常／成果保持原位置。
- 修复返回会话时的阅读位置判断、跨请求重复工具 ID 的交互定位，以及重分组后的链接／表格焦点。内容区仍有焦点时保持展开，summary 按钮可正常手动收起。

桌面运行中、折叠、展开截图已查看，保存在被 Git 忽略的 `experiments/harness-lab/.local/validation-w01-10/`。既有移动端回归沿用，没有新增手机端产品要求。

## 发布

- 唯一服务：`tencent-server` 的 `127.0.0.1:4315`，4316 未启动。
- 当前版本：`/home/ubuntu/axon-release-w01-10.yRv9Jh/retry`。
- 完整数据备份：上述目录的 `backups/4315`；旧版目录仍保留，可回滚。
- 历史副本预检及发布后核对通过：两席位各 2 个会话，历史消息、Agents 记录、业务交接保持一致；原有 50 个数据文件全部字节不变。
- `deepseek-flash` 配置与 Docker 执行环境可用；页面 HTML、JS、CSS 与构建产物字节一致。
- 资源：`index-B0CsJaeb.js`、`index-xwFoGB11.css`。
- 发布包 SHA-256：`1ae0e80248b2d8fc20cf99fcaf43cf08f6b02e6eaee0ecd4750fd8fbe194344e`。

首次发布预检拦截了 macOS tar 生成的 `._*.md` 元数据文件，自动恢复旧版，原数据未改动。改用不携带元数据的打包方式后重新预检、发布成功。首次失败日志和备份保留在上级发布目录，未放宽角色校验。

本轮仅调整展示和只读快照字段，没有使用真实用户任务重发模型请求。

## 2026-09-21 展示微调

按用户要求移除每条回复上方的头像与 Axon 名称，并删除对应显示参数、去重逻辑和样式。类型检查、lint、构建及既有聊天展示浏览器回归（1/1）通过。已同步唯一 4315 服务的静态页面，无需重启；旧前端备份位于当前发布目录的 `chat-brand-20260921/backup`。新资源为 `index-faKIVRge.js`、`index-CYhM5cHL.css`，HTTP 返回内容与构建产物一致。随本轮改造提交。
