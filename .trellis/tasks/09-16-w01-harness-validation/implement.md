# W01 实施计划

日期：2026-09-16。基线：MVP v0.7。状态：2026-09-16 用户授权实施 W01-1，任务 in_progress；S0／S1 已实现并完成 C01～C06 真实／本地组合验收；W01-2（S2a）和 W01-3（S2b）已分别完成 A01～A12、B01～B13 组合验收；W01-4（S2c）、W01-5（S3a）与 W01-6（S3b）已实现，其余 S3～S5 未实施。结果见 [验证记录](research/validation-results.md)。

## 1. 分阶段实施

S0 + S1 是可独立交付的 W01-1，以 C01～C06 退出。随后 S2 完善 Harness 能力，S3 引入完整运行管理与成果，S4 验证可替换边界，S5 收敛长期路线。完整 T01～T13 不作为第一步退出条件。首步按 [design.md](design.md)，后续按 [design-later.md](design-later.md) 的阶段标记实施。

### S0 最小工程与接入准备

- [x] 审阅最新首增量设计后进入实施；读取适用规范，不把占位模板当成既定架构。
- [x] 在 `experiments/harness-lab/` 建立 TypeScript／Node、React／Vite、Fastify 的最小工程；锁定实际依赖版本，核对 Pi 公共 API、安装及许可。
- [x] 设置独立 `src/pi` 接入模块、最小 HTTP／页面字段和固定资料；关闭默认危险工具及全局资源发现，使用回环地址和受控实验目录。
- [x] 准备 `.env.example`、类型／静态检查与基础测试命令；密钥在 `.env.local`，原生会话文件在 `experiments/harness-lab/.local/sessions/`，均排除 Git。
- [x] 首次真实调用前告知用户准确的模型配置字段，确认非敏感端点、model ID、thinking 模式和测试预算，再由用户本地配置密钥。

S0 仅解决对话入口所需准备，不安装 SQLite，不建设平台 Run／快照／操作账本、完整 HarnessAdapter、确认恢复或导出测试框架。

### S1 首个可用增量 W01-1

- [x] 用 Pi 原生会话机制创建／列出／加载会话，隔离各会话历史；单一本地测试主体，至少两个会话。
- [x] 提供发送消息、流式回复、查询历史和停止当前 requestId 的入口；同会话忙时拒绝第二条输入，结束／取消收敛后可继续。
- [x] 接入唯一 `source.read` 只读资料工具；模型决定是否调用，工具结果进入后续对话。
- [x] 实现会话列表、对话、折叠工具结果、停止／错误提示；保证中文输入与未发送草稿，避免切会话时串入事件。
- [x] 复用原生会话文件保存已完成历史；刷新只查状态／历史，活动请求可轮询快照，不建设可靠 SSE 回放或命令自动重发。
- [x] 设置基本超时、工具次数与输出限制，验证取消收敛、模型错误和迟到事件；不建立持久化运行预算账本。
- [x] 完成 C01～C06 的真实对话／工具／取消、历史重启续聊、隔离和边界证据。

退出：用户能真实连续对话、调用只读工具并追问，切换会话、停止和读取已完成历史。无模型密钥可以完成部分本地检查，但不能把真实路径记作通过。此时可演示并独立交付首增量，不等待 T04 确认或 T13 导出通过。

### S2 Harness 能力扩展

拆分为可独立审阅的增量：S2a [W01-2 工作区、Memory 与 Skill](../archive/2026-09/09-16-w01-2-workspace-memory/review.md)（已完成，A01～A12 通过）；S2b [W01-3 上下文管理与压缩](../09-17-w01-3-context-compaction/review.md)（Pi 默认压缩已实现并通过 B01～B13 组合验收）；S2c [W01-4 子 Agent 委派与协作](../09-17-w01-4-subagent/review.md)（基础多角色配置、single 只读委派、Pi SDK 复用和父子关联；已实现，验收见阶段记录）。

- [x] S2a 先落地工作区、会话固定归属及原有会话过渡，再接入指令与 Skill；不引入业务 Task／WorkItem。

- [x] 扩充受控工具和固定 Skill，验证同一工具集合在不同目标下的组合；成果数据库及版本化写入后移至 S3。
- [x] 复用 Pi 资源加载，加入工作区 AGENTS.md、受控文件编辑、下一请求重载和范围校验；记忆仍为文件，不建设条目库。
- [x] 按 W01-3 接入 Pi 默认压缩，保留既有 AGENTS.md 加载和网页原文；移除默认整轮次数／时长硬限制，分别配置模型／工具超时与可选总时限；验证续作、停止及有界失败／恢复。
- [x] 按 W01-4 加入基础多角色配置，由主 Agent 按职责选择，以独立 Pi 会话、原生历史和父子请求关联验证角色工具范围、委派、综合、取消、用量与禁止递归；显式父时限覆盖子任务，正式预算账本仍在 S3。
- [ ] 验证 T06～T10 的能力部分，记录当前未覆盖的正式 Run、平台记录和成果行为；进入 S3 后完成对应全量复验，不提前将整项 T 标为通过。

本步继续使用原生会话保存；S2a 引入受控工作区索引与会话归属，仍为单一本地主体，不扩展为业务任务管理。S3 的表和状态机不作为本步前置条件。

### S3 文件作业与运行管理

先按 [W01-5 Web 通用文件作业](../09-17-w01-5-artifact-versions/review.md) 实施 S3a（已实现并通过 E01～E15 组合验收）：

- [ ] 建立任务 × 席位工作区，复用 Pi 公开文件／命令工具，接入最小容器沙盒、Python 脚本和中间文件。
- [ ] 网页附件／拖放、流式上传成功即普通文件、当前席位统一文件面板、只读子 Agent 文件访问及预览／下载交付。
- [ ] 验证同工作区多会话并行、各请求独立取消和进程清理；保留原生会话及旧历史兼容，不新增成果 SQLite 或同文件冲突保护；完成 E01～E15，不提前标记完整 T 通过。

S3b [W01-6 Web 人工交互：提问与操作确认（HITL）](../09-18-w01-6-hitl/review.md)，已实现并通过 F01～F18 组合验收：

- [ ] 复用 Pi 固定扩展和工具钩子，接入 AskUser、操作确认／拒绝、请求停止、刷新和历史；bash 按服务端规则检查实际参数，沙盒不变；完成 F01～F18，演示工具仅补充故障验证。
- [ ] 沿用进程内等待和原生会话；不实现真实上报／下发、跨席位审批、完整 Run、等待资源释放或重启接续。

以下完整平台运行工作另拆后续 S3 增量，进入前重新审阅，不作为 S3a／S3b 前置条件：

- [ ] 根据已有真实调用收敛平台 Session／Run、事件、授权与工具协议；引入实验数据库、输入快照、操作账本、成果版本与平台工作记录。
- [ ] 备份原生实验会话，验证过渡至平台存储的方式；无法安全转换的旧会话保留只读并通过显式关联继续，不静默丢弃或重放。
- [ ] 落地完整 Run 状态机、确认暂停释放执行资源、同 Run 执行段续接、取消竞争与中断识别。
- [ ] 加入 `artifact.read/save_draft/submit`、两个测试任务与身份、写操作幂等和版本校验，完成草稿 v1／v2 及确认场景。
- [ ] 建立可靠事件保存／回放、父子 Run 和持久化预算，注入写入成功但快照未提交、重复确认、SSE 断线及取消竞争等故障。
- [ ] 完成 T01～T11；S2 已验证行为在正式平台记录／控制接入后按影响复验。

完整运行控制的具体设计在 [后续设计第 5 节](design-later.md#5-run-与交互状态)。本阶段才验证正式恢复承诺，首步原生历史通过不替代此项。

### S4 可替换边界验证

- [ ] 在已有调用基础上收敛最小 HarnessAdapter，保持 Pi SDK 类型、hook 与原生格式只在适配层使用。
- [ ] 补齐平台工作记录、受控 ProviderRecord、版本化 NativeCheckpoint、内核绑定与不兼容版本拒绝。
- [ ] 实现受控本地导出／只读读回，验证完整消息、工具调用／实际结果、指令／Skill 版本和成果引用；不执行工具或开放凭证／内部推理。
- [ ] 用最小契约替身和依赖规则完成 T13，说明其只证明边界与导出成立，不等于自研或跨内核迁移已实现。

### S5 技术收敛与交接

- [ ] 汇总 C01～C06 和 T01～T13 的分期结果、环境／模型／依赖版本、记录引用与限制；失败先修正，不把跳过视为通过。
- [ ] 用 T12 汇总原生能力、宿主补齐、维护和退出依赖成本，形成 M1 内核、数据库与主要框架的选择记录；首步使用 Pi 不等于长期选型已通过。
- [ ] 说明正式身份、API／Worker 分离、可靠作业、协同、部署和领域接入的后续工作；不提前标记 FG／AC 通过。
- [ ] 根据实现事实更新相关规范，运行必要质量检查；需要提交时使用中文说明，保留其他任务及用户新增文件。

## 2. 验证入口

以下命令已在实验工程建立；具体执行结果见验证记录：

```bash
npm --prefix experiments/harness-lab ci
npm --prefix experiments/harness-lab run typecheck
npm --prefix experiments/harness-lab run lint
npm --prefix experiments/harness-lab test
npm --prefix experiments/harness-lab run test:e2e
npm --prefix experiments/harness-lab run probe:live -- --case C01
npm --prefix experiments/harness-lab run dev
```

普通测试用确定性替身验证当步边界，不访问真实 API。S1 的端到端测试使用隔离的 Pi 会话目录；SQLite 故障测试在 S3 再加入。`probe:live` 显式执行且要求模型配置；用户暂不设置验收总请求数上限；S1 保留原实验限制记录，S2b 运行控制按 W01-3 设计及独立探针配置执行。首增量覆盖 C，后续按阶段扩展 T；不要求 S0／S1 预先实现完整探针框架。

每步运行受影响检查；模型测试保留必要证据，避免无关重复调用。首增量只报告 C01～C06，完整 T、FG、AC 仍保持独立的验收状态。

## 3. 数据、证据与过渡

S1／S2 的历史保存来源为受控 Pi 原生会话文件；不另建平台快照副本。日志只使用会话／requestId，真实模型结果记录配置标识与版本，密钥不进入日志。脱敏结果写入实施后创建的 `research/validation-results.md`。

S3／S4 引入数据库前先备份原生会话，明确转换／只读保留方式，之后避免双重可写历史。Pi 升级先检验已有完成会话，异常回合不自动重发。未来自研的完整迁移与回退仍按 [后续设计第 11 节](design-later.md#11-故障验证与迁移边界) 另行实施。

## 4. 当前规划交付

- [x] 将第一步收敛为 Pi 多轮对话、最小只读工具及基本交互控制。
- [x] 新增 C01～C06 独立验收，保留 H01～H08 与 T01～T13 并标明实施阶段。
- [x] 将完整 Run、平台数据契约和迁移边界移到后续设计，重新编排 S0～S5。
- [x] 更新审阅入口：先审首步范围与使用行为，完整运行和自研迁移细节后续再收敛。

上述规划文件已完成，W01-1 已进入实施；代码和本地检查进度见验证记录。真实联调已使用用户配置的 API Key 完成；Ubuntu 凭证在部署验证前再提供。

## 5. 规划核验记录

2026-09-16 已执行：Trellis context validate（两份清单各 6 项通过）；13 份相关 Markdown 的本地链接、表格、代码围栏及编号检查（51 个本地链接有效，H01～H07／T01～T12 完整）；task.json／JSONL 解析；`git diff --check`。任务仍为 planning，未运行应用测试或真实 API 探针。

用户澄清 Memory 后，已按 MVP v0.5 再次通过同项文档检查与 Trellis 清单校验；旧 Memory 条目／召回／采纳接口已撤去，R13／FG08／AC21／T06 改为指令文件验收。内核选型仍在讨论，未启动实现。

v0.6 文档核验：14 份 Markdown、73 个本地链接及 10 个章节锚点有效；表格、代码围栏、空白、JSON／JSONL 解析、两份 Trellis 上下文清单各 6 项及 `git diff --check` 通过。R01～R15、FG01～FG11、AC01～AC24、H01～H08、T01～T13 定义连续且无重复，新增需求到工作包／探针的映射已复核。已通读收敛后的 PRD；候选与行为约定集中在 review.md 等待审阅，未宣告规划批准或能力探针通过，任务保持 planning。

v0.7 文档核验：15 份 Markdown、82 个本地链接和 13 个章节锚点，表格／围栏／空白、JSON／JSONL、两份 Trellis 上下文清单各 6 项及 `git diff --check` 通过。新增 C01～C06 连续且无重复；与本轮修改前逐行比较，R／FG／AC／H／T 的既有验收内容保留，T 仅增加实施阶段列。首步设计不再包含完整 Run 状态表，S1 与后续 S2～S5 的数据、交互和验证边界已复核；已通读收敛后的 PRD。任务仍 planning，无应用测试或真实 API 调用。

## 6. W01-1 验收结果（2026-09-16）

C01～C06 已通过本地检查与真实 DeepSeek Flash 验证：13 条探针请求、2 条实际浏览器请求；包含工具读取、隔离、真实取消和独立 Node 进程重启续聊。证据与限制见 [验证记录](research/validation-results.md)。仅 S0／S1 完成；不启动后续 S2～S5，不标记完整 W01、FG 或业务 AC 通过。
