# W01-8 实现与验证记录

日期：2026-09-19。实现及正式发布已完成；用户使用验收待进行。

开工前基线：`fbcdd46 保存任务分派与成果交接设计及界面简化基线`。实现未推送；2026-09-19 经用户授权更新线上 4315，未恢复已停用的 4316 实例。

## 实现范围

- 一个服务内切换两个测试席位，各自的工作区、会话、指令、文件和浏览器草稿保持范围隔离。未配置双席位时保留旧单席位模式。
- 工作待办及详情、目标与多份输入资料分派、明确签收、新建或关联办理会话、单份成果提交、验收／退回和再次提交。
- 页面与 Agent 共用协作服务；Agent 复用现有 Pi 工具循环及 HITL。提交前预览固定文件，提交后保留版本与回执，业务等待不保持 Agent 运行。
- SQLite 仅保存本期工作、准备单、提交、关联和回执；原 Pi JSONL、压缩和中断后显式继续机制保留。
- 现有手动选择／指定文件继续使用；`@` 弹出文件选择列表留到后续阶段。

## 自动化与故障验证

| 范围 | 证据与结果 |
| --- | --- |
| 类型、代码规范、模块依赖、构建 | typecheck、lint（含边界检查）、build 通过；构建有既有单包体积提示，不影响产物 |
| 单元／集成 | 全量 30 文件 354 项通过，随后新增单席位停服备份升级用例，所在文件 6 项通过；共 355 项已验证（新用例覆盖停服备份升级） |
| 业务服务 | 18 项覆盖完整状态闭环、固定字节、revision 竞争、重复回执、事务回滚、损坏数据、权限及安全导入 |
| Pi 集成 | 8 项覆盖原生工具循环、精确确认、拒绝／取消、子 Agent 无写交接能力、业务回执查询与原始历史读回 |
| 席位 API | 6 项覆盖现有读写／上传／下载／交互范围、会话归属、单实例索引以及旧单席位数据副本升级 |
| 进程故障 | 真实 SIGKILL 覆盖复制进行中、准备完成、等待确认、业务提交前后及工具结果尚未保存；无虚构提交、无自动重放；成功业务回执与旧原生缺结果状态分别保留 |
| 浏览器 | 全量 66／66 通过，包含 5 项使用实际 PiLab／SQLite 服务的交接用例；桌面及 375px 截图检查无溢出 |

复制故障使用实际文件复制原语并在首块写入后终止父进程；孤立的部分副本不出现在业务元数据中，接收方访问返回 404。导入取消会等待受控文件发布收尾，避免 hardlink／unlink 间强杀留下不合法文件。

旧数据验证先关闭单席位服务，复制独立备份，再启用双测试席位打开。原工作区和会话 ID 保留；原 JSONL、AGENTS.md 及普通文件按字节对比不变；无模型调用或消息重放。

网页回归发现并修复两项实际问题：手机新建对话弹窗关闭后的焦点时序，以及首条消息创建会话时旧输入回调将下一轮草稿写回旧工作区键。后者仅在渲染交接窗口内修正归属，不新增持久状态；原失败用例连续三次通过，未放宽断言。prepare／commit 响应丢失均查询原操作，不自动重发。

## 真实模型与执行环境

环境：`tencent-server` 独立验证目录，Node 24.14.0、Pi 0.85.1、DeepSeek `deepseek-flash`，Docker 镜像 `berserk-file-runtime:w01-5`。仅读取既有模型配置；没有复制凭证到仓库或修改生产数据。测试使用 `app.inject`，没有启动第二个正式监听实例。

`scripts/probe-handoff.ts` 的五次真实用户请求全部成功：

1. A 分派目标、任务书和 CSV，两份文件通过真实上传接口进入工作区，批准后形成 B 的待办。
2. B 签收并关联会话，导入资料，生成并实际执行 Python 脚本，将数量 3＋5 计算为 8，生成方案并确认提交。
3. A 退回并要求补充风险提示。
4. B 根据本轮工作信息读取退回意见、修改方案并再次确认提交；前后两个固定版本内容不同，第一版仍可读取且未变化。
5. A 验收当前第二版。服务关闭／重开后，工作完成状态、双方原生会话和关联均可读回，没有重放操作。

同时核对确认卡片刷新可查询、重复批准不重复提交、页面不能绕过 Agent 准备单门禁、另一席位不能读取原私有工作区。B 的 Python 文件和成功 Bash 工具结果均存在，不以助手文字代替执行证据。

完整证据保留在服务器 `/tmp/axon-handoff-live-qst4pS/handoff-evidence.json`，本地副本位于被 Git 忽略的 `experiments/harness-lab/.local/validation-w01-8/handoff-evidence.json`。SHA-256：`a921539ec82e7a325361dffc3c80d9f1205151ccf789f75d50dd6c1f3e0bc66d`。

验证中发现模型有一次停在 prepare 后错误声称已展示确认卡片。已在 prepare 工具说明中明确“准备不显示卡片，须继续调用 commit”，随后上述五轮真实闭环通过；没有新增自动代提交机制。诊断脚本仅对已核对的固定测试命令批准 Bash 卡片，不修改产品的命令规则。

## 验收范围与交付边界

H01～H14 对应的席位、业务、固定文件、确认及故障机制已由上述组合验证覆盖。H15 的网页交互经全量浏览器回归通过。H16 的独立真实模型／Docker闭环、旧数据备份读回、回归及正式停服备份／唯一 4315 发布检查均已通过，发布记录如下。

本期尚不是人员登录、外部调度或完整业务平台验收。功能与部署组合验收通过，任务仍待用户使用验收。

## 正式发布（2026-09-19）

用户授权更新后，仅更新 `tencent-server` 的 `127.0.0.1:4315`。发布目录 `/home/ubuntu/axon-release-w01-8.8YJxlX/app`，继续使用原 `browser-data` 和原模型配置。打包的是本期已验证工作树，尚未新增实现提交或推送；归档 SHA-256 为 `27b945af322160c39c22db46d722ab318623a439dcdf7f6baa5ffa7f910468ba`。

- 旧服务无活动请求后正常退出；完整停服备份位于发布目录 `backups/4315`，备份清单与原数据一致。先以独立 `verification-data` 副本启用双席位，验证旧会话、模型与 Docker 再发布。
- 原清单 29 个文件中，28 个文件内容保持不变；workspace-index 仅新增 B 的工作区，旧工作区与会话归属保留。原 1 个会话可读、JSONL 不改写、不重放消息。
- 新 PID 为 434528；仅 4315 监听，4316 未运行。两个席位的业务查询正常，B 不能读取 A 的原会话。模型 `deepseek-flash` 已配置，Docker 可用。
- HTML 与本地构建一致（`index-Dcz5WAET.js`）；通过本地 SSH 入口实际浏览，席位切换、原历史和工作待办均正常。未在正式数据中创建测试分派或调用模型。
- 发布目录保存 `deployment-result.json`、`backup-manifest.json`、日志与 `start-4315.sh`。回退应停新服务，以完整备份恢复到独立目录，再运行旧版本，不用旧版直接写入新数据。

### 设置入口精简发布

按用户要求移除右上角模型设置，设置统一在侧栏左下角，配置提示指向该入口。类型检查、Lint、构建与4项相关浏览器回归通过。用户授权后更新同一4315的前端资源：`index-Bc36OikC.js` / `index-DP-MdACi.css`；先发布资源再原子替换HTML，旧资源保留。后端未重启，数据未改动。旧前端备份位于发布目录 `backups/settings-ui-before`；HTTP逐字节校验及实际浏览器检查通过。
