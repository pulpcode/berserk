# 验证结果

日期：2026-09-17。实施范围为已确认的 A（分组侧栏）＋ C（全部动态）。

## 交付

- 分组侧栏、工作区折叠、活动／关注计数、直接跨区选择和空区进入。
- 全局动态及全部／处理中／需关注筛选；轻量 GET /api/activity、真实执行阶段、更新失败提示。
- 当前工作区的新建对话与输入目标；独立草稿、每会话阅读位置、前台阅读到末尾才标为已读。
- 保留既有资料、指令冲突、Skill、历史详情、取消和中文输入功能；未增加模型工具或跨会话上下文共享。

## 自动验证

| 验证 | 结果 |
| --- | --- |
| npm run typecheck | 通过 |
| npm run lint | 通过，零警告 |
| npm test | 7 个文件、48 项单元／集成测试通过 |
| npm run test:e2e -- --workers=2 | 最终完整运行 18 项全部通过 |
| npm run build | 通过 |
| git diff --check、Trellis 上下文校验 | 通过 |

新增浏览器场景包括：未打开会话的跨区状态、不批量获取正文、折叠计数、全局筛选、完成与未读分离、刷新保留已读、旧摘要与新 SSE 竞争、更新失败后恢复、阅读位置及草稿保留、窄屏导航、创建工作区去重，以及摘要先于正文时停止按钮的就绪判断。

独立检查修复了两项确定问题：创建响应晚于全局轮询导致重复工作区；摘要先于正文时停止按钮看似可点但没有匹配快照。复测暴露了后一项时序问题，保留 revision 防护，修复操作就绪判断，并加入受控延迟的确定性测试；最终完整回归通过。

## 实际页面检查

使用独立浏览器上下文只读访问当前 127.0.0.1:5173，检查桌面 1440×1000 和窄屏 375×812 的分组侧栏／全部动态；无水平溢出、无 pageerror。未向用户现有会话发送消息，未修改工作区数据。

本次不涉及模型行为变更，未调用真实模型验收或消耗 API 额度。现有 probe-workspace 的旧下拉框选择器已同步为新工作区按钮，通过类型与静态检查，未把未运行的真实探针记为通过。

## 边界

已读记录和草稿在当前标签页 sessionStorage 中；阅读位置为页面内存状态，刷新后不承诺恢复滚动位置。状态约 1.8 秒刷新，不是完整的持久化 Run。双会话分屏、任务分派和跨会话检索仍未纳入本次实现。
